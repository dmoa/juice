#pragma once

enum ENTITY_TYPE {
    PLAYER_TYPE,
    MAP_TYPE,
    ENEMY_TYPE
};

enum ENTITY_NAME {
    PLAYER,

    TREE1,
    TREE2,
    LOG,
    STONE,

    SPIDER
};

enum ANIMATION_TYPE {
    IDLE,
    RUN,
    ATTACK
};